import type { InvokeZoomDeps, ResetZoomDeps, ZoomToDeps } from "./zoom-utils";

export function buildZoomToPointDeps(deps: ZoomToDeps): ZoomToDeps {
  return deps;
}

export function buildResetZoomDeps(deps: ResetZoomDeps): ResetZoomDeps {
  return deps;
}

export function buildInvokeActiveZoomingDeps(deps: InvokeZoomDeps): InvokeZoomDeps {
  return deps;
}