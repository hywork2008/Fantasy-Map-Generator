# City Editor 生成アルゴリズム改善 — 実装計画

`docs/city-editor/改善計画.md` と `docs/city-generator/towngen-comparison.md`（分析）を受けた
フェーズ分けの実装計画。`design.md §8` のマイルストーン（E0–E5）とは独立の **G シリーズ**。

## 決定事項（改善計画より）

| 項目 | 決定 |
| --- | --- |
| 手本 | TownGeneratorTS `Model.ts` / `CurtainWall.ts` / `Topology.ts` / `Voronoi.ts`（GPL・読むだけ・関数単位で再実装） |
| 変更コードの置き場所 | **`src/city-editor/core/gen/`** に再実装。`generate.ts` の `src/city-generator/*` 依存を断つ |
| `src/city-generator/` | **凍結・参照専用**。修正しない。commit `1b7f70188` / `120973c4f` で旧側に入った改良は移植時に City Editor へ持ち込む |
| standalone `/city/` ページ・FMG Burg エディタの `openCityGenerator()` | Phase G6 まで放置（凍結）。切替は最後 |
| 決定論 | 全 stage が `(document, settings, seed, step)` で不変。seed 内部・非表示（既存方針） |
| GPL 境界 | TownGeneratorTS のコードは貼らない。`city-generator`（MIT）からのコピーは可だが改良を旧側へ戻さない |

## フェーズ依存関係

```text
G0 (vendoring / 依存断ち)  ─┬─> G1 (セル整形 + Grid evolution)  ── 本命・土台
                            ├─> G2 (現実性フィルタ + 街道)      ── 最優先バグ（海）
                            ├─> G3 (門オプション)
                            ├─> G4 (河川オプション)
                            └─> G7 (河川辺の排他)               ── 同一 edge 重なり
G1 ─> G5 (平滑化仕上げ)
G0..G5, G7 ─> G6 (city-generator 撤去 / ページ切替)
```

G0 は全フェーズの前提。G1〜G4 と G7 は G0 の後は並行可。G2 と G7 はどちらも
`gen/streets.ts` の A\* `hardBar` を触るので、同時実装するときは合成（論理和）にすること。

---

## Phase G0 — 生成コアの vendoring と依存断ち（機能変更なし）

**目的**：`src/city-editor/` から `src/city-generator/` への import を 0 にする。挙動は
ビット単位で不変（純粋な移設）。

**現状の依存**（`grep -rn city-generator src/city-editor`）:

| ファイル | 使っているもの |
| --- | --- |
| `core/document.ts` | `buildGrid`, `makeRng`, `CityGeography` / `CityParams` 型 |
| `core/mesh.ts` | `Cell` 型 |
| `core/generate.ts` | `synthSite`・`siteToGeography`・`siteToProgram`・`randomSiteConfig`・`resolveWallPlan`・`classifyRiver`・`classifyCoast`・`classifyUrban`・`buildEdgeGraph`・`geom`・`interior`（`placeGates`/`placePrecincts`/`markWaterGate`/`markSeaSurroundedGates`）・`prng`・`riverPath`・`streets`・`wards` ほか型 |
| `core/generate.test.ts` | 同上のテスト用 import |

**アプローチ**

1. `src/city-editor/core/gen/` を作り、上記が参照する **純粋関数モジュール** を
   `city-generator/core` / `city-generator/site` から**バイト単位でコピー**：
   `gen/{types,prng,geom,voronoi,grid,edgeGraph,graphWalk,classifySea,classifyRiver,classifyUrban,riverPath,streets,wards,interior}.ts`（14）
   と `gen/site/{burgSiteDescriptor,presets,siteConfig,siteInput,synthSite}.ts`（5）。
   - ex-`core` の 14 ファイルは相対 import が `./sibling` のみなので**無編集**。
   - ex-`site` の 3 ファイル（`siteConfig` / `siteInput` / `synthSite`）だけ
     `from "../core/x"` → `from "../x"` に置換（計 7 行）。ロジックは無変更。
   - `pipeline.ts` / `buildings.ts` / `render/*` / `ui/*` / `index.ts` は**コピーしない**
     （`generate.ts` の `runPlan` が自前パイプライン。S7 建物は未使用。必要になった
     フェーズでその時コピーする）。
2. `document.ts` / `mesh.ts` / `generate.ts` / `generate.test.ts` の import を `./gen/*` に差し替え。
   `generate.ts` の barrel import は真のソースモジュール（`gen/types` / `gen/site/siteConfig`
   / `gen/site/siteInput` / `gen/site/synthSite`）へ展開。`SiteConfig` の再エクスポートは維持。
3. ガードテスト `core/gen/boundary.test.ts`：`src/city-editor/**` の `.ts` が
   `from "…/city-generator/…"` を含まないことを assert。
4. `biome check --write src/city-editor`（import 並べ替え）／`lint:legacy` ／`tsc` を通す。

**受入条件**

- `src/city-editor/**` に `/city-generator/` を指す import/export が 0（`boundary.test.ts`）。
- `generate.test.ts` / `CityEditorPage.generate.test.ts` / `mesh.test.ts` / `features.test.ts` /
  `svg.test.ts` が**ロジック無変更で**緑（import パスのみ修正、挙動不変の証拠）。
- `diff` で ex-`core` 14 ファイルが `city-generator/core/*` と完全一致、ex-`site` 5 ファイルは
  上記 7 行のみ差分（＝挙動がビット単位で不変）。
- `tsc` 0・`biome` クリーン・`vitest` 全緑。
- ブラウザ：`/city-editor/` で New grid → ①〜⑥ → ◀/▶ が従来どおり動く。

**テスト**：`boundary.test.ts` を追加。`city-generator/core/*.test.ts` の移設は G0 では**行わない**
── それらは `generateCity`（`pipeline.ts`）依存で、コピーには未使用の `pipeline.ts` /
`buildings.ts` 一式が必要になる。回帰は (a) City Editor 自身の 183 テスト（実 `runPlan` 経路を
網羅）と (b) 上記 `diff` 一致で担保。`gen/` が実際に分岐する G1 以降、その時点で必要な
ユニットテストを `gen/` 配下へ用意する。

**G0 実施結果（2026-09-03）**：完了。`gen/` 19 ファイル vendored（core 14 = バイト一致、
site 5 = import パス 7 行のみ差分）、consumer 4 ファイル rewire、`boundary.test.ts` 追加、
`gen/LICENSE-NOTE.md` 追加。検証：`tsc` 0、`biome` クリーン、`lint:legacy` pass、
`vitest src/city-editor src/city-generator src/city` = 29 files / 404 tests 緑（city-editor は
既存 182 が無改変で緑 ＋ boundary 1）。ブラウザ未確認。

---

## Phase G1 — セル整形の TownGen 型再実装 ＋ Grid evolution ＋ 母点編集（本命）

**目的**：改善計画「TownGeneratorTS のセル整形を for ループ 1 周単位で見たい／Grid evolution
スライダーのセル修正アルゴリズムを改善する為／頂点座標の位置・数を指定・編集したい」を満たす。

**手本**：`Model.buildPatches` [Model.ts:167-222]、`Voronoi.build` / `Voronoi.relax`
[Voronoi.ts:31-207]、`Model.captureGenerationStep("2.1 Delaunay / Voronoi")` [Model.ts:154]、
`SvgMapLayer.drawDraft`（delaunay 赤線＋ sites 赤点）[SvgMapLayer.ts:228-247]、
`TownApp` の "Grid evolution" スライダー。

**対象**

- 新規 `core/gen/patches.ts`・`core/gen/gridEvolution.ts`・`core/gen/voronoi.ts`（コピー済みを流用可）
- `core/document.ts`（`createDocument` の格子ソース切替）
- `ui/CityEditorPage.ts`（Document パネル：スライダー・レイヤートグル・母点入力・Voronoi sites tool）
- `render/svg.ts`（delaunay / voronoi / sites オーバーレイ、grid-evolution `<g>`）
- `core/types.ts`（`Tool` に `"site"` 追加 — design §4.1 に既記載）

**アプローチ**

1. **`gen/patches.ts`**
   - `scatterSites(nPatches, rng)`：`for i in 0..nPatches*8` → `a = sa + √i·5`、
     `r = i===0 ? 0 : 10 + i·(2 + rng())`。中心密・外周疎。窓スケールへ線形マップ
     （TownGen は無次元、City Editor は `frame.extentMeters` に正規化）。
   - `buildIncremental(sites)`：`gen/voronoi.ts` を 1 点ずつ `addPoint`。各追加で
     `{ sites: [...soFar], delaunay: edges(), cells: partition() }` を stage 配列へ push
     （重い場合は「N 点ごと」に間引くオプション。既定は 1 点ごと）。
   - `relaxCentral(voronoi, K, passes)`：距離順先頭 `K` 母点だけ重心へ寄せて `Voronoi.build`
     し直す（TownGen `[0,1,2,nPatches]` = K≈4 相当）。各 pass を stage に push。
   - `orderByRadius()`：`sites.sort(by |p|)`。先頭 `N` を `Cell.site` 順に返し、`meshFromCells`
     が urban 候補を id 昇順で持てるようにする。
2. **`gen/gridEvolution.ts`**：`buildGridEvolution(params, settings) → GridStage[]`
   （`towngen-comparison.md §3.A` の型）。scatter 各点 + Voronoi 各追加 + relax 各 pass +
   （任意で）Lloyd 各 pass。ラベルは `"scatter 42/120"` / `"addPoint 42/120"` /
   `"relax pass 2 (K=4)"` / `"Lloyd 1"`。
3. **`document.ts`**：`createDocument` は `buildGridEvolution(...).at(-1).cells` →
   `meshFromCells`。`buildGrid`（`gen/grid.ts`）は当面 fallback として残すが既定は patches 型。
   `CITY_SIZE_PRESETS` の `cellsAcross` は `nPatches` へ読み替え（`nPatches ≈ (cellsAcross/3.5)²`
   目安、実測で調整）。
4. **Document パネル UI**（Generate パネルの step 行と同じ部品を流用）
   - `nPatches` 数値入力（母点数）。`relax K` と `relax passes` の数値入力。
   - 「Grid evolution」range スライダー ＋ `◀ First / Prev / Next / Last ▶` ＋ ラベル output。
     スクラブ中は `documentState` を触らず、`renderEditorSvg` に stage オーバーレイを渡して
     プレビュー描画（履歴を汚さない）。
   - レイヤートグル：☑ Delaunay ☑ Voronoi ☑ Sites。
   - 「この格子を採用」ボタン：現在 stage の `cells` を `meshFromCells` → 新 `mesh` を
     `history.commit(_, "Rebuild grid")`。feature group がある文書では design §5.1 の移管
     プレビュー（今回は「全 feature 未移管扱い・確認必須」で可）。
5. **Voronoi sites tool**（`tool === "site"`）
   - 母点 add（空きクリック）／ move（ドラッグ）／ delete（右クリック）。
   - 変更のたびに `gen/voronoi.ts` で再計算しプレビュー。`Enter` で確定 → `meshFromCells`。
   - `Face.site` を真実源にし、`meshFromCells` は `cell.site` を受け取れるので既存経路で足りる。
6. **`render/svg.ts`**：`.ce-grid-evolution` `<g>` に delaunay（細い赤線）・voronoi（半透明セル）・
   sites（小円）。`renderEditorSvg` の引数に `gridStage?: GridStage | null` を追加。

**受入条件**

- Document パネルのスライダーで、母点散布 → 逐次 Voronoi → relax → （Lloyd）が
  **for ループ 1 周単位**で前進し、各段の Delaunay 三角形・Voronoi セル・母点が見える。
- `nPatches` を変えるとセル数が比例、`relax K` を 4 → 全点にすると中心以外も動くのが見える。
- Voronoi sites tool で母点を 1 個足す／動かす／消すとセルが即再計算され、`Enter` で
  メッシュが置換され、undo で完全復元。
- 既定の New grid が「中心セル最大・外周セル小」（TownGen 型のサイズ勾配）になる。
- `tsc` 0・`biome`・`vitest` 全緑。ブラウザで console エラー無し。

**テスト**

- `gen/patches.test.ts`：決定論、`nPatches → セル数` 単調、i=0 セルが原点を含む、中心セル面積 >
  外周セル面積（勾配）、`relaxCentral(K=4)` は先頭 4 母点だけ移動。
- `gen/gridEvolution.test.ts`：stage 数 = 散布点数 + Voronoi 追加数 + relax passes（+ Lloyd）、
  隣接 stage 間で `sites` が高々 1 点しか増えない／`relax` stage は座標だけ変わる。
- `CityEditorPage.generate.test.ts` 追記：スライダー DOM、`◀/▶` で stage index が
  `[0, total-1]` にクランプ、「採用」で `mesh.faces` 数が stage の `cells` 数と一致、undo 復元。

**G1 実施結果（2026-09-03）**：**コア完了**。

- 新規 `core/gen/patches.ts`（`scatterPatchSites` = TownGen 螺旋散布を窓へスケール、
  `relaxCentralSites` = 中心 K 点だけ重心へ、`buildPatchCells` = 散布→guard→Voronoi→relax
  ×passes→窓クリップ）、`core/gen/gridEvolution.ts`（`buildGridEvolution` = 散布 1 点／
  relax 1 pass 単位の `GridEvolutionStage[]`、`.at(-1).cells` は `buildPatchCells` と
  バイト一致 ── テストで担保）。
- `core/gen/voronoi.ts`：`computeDelaunayEdges` を追加（G0 の「city-generator/core と
  バイト一致」不変を初めて破る＝想定どおり。純加算）。
- `render/svg.ts`：`GridOverlay` 型 ＋ `.ce-grid-evolution` オーバーレイ（Voronoi セル／
  Delaunay 辺／母点、レイヤートグル対応）。`renderEditorSvg` に `gridOverlay?` 引数。
- `ui/CityEditorPage.ts` Document パネル：`Patches (nPatches)` / `Relax K` / `Relax passes` の
  **スライダー**（値読み出し付き。ドラッグ中の `input` ごとに `runGridEvo()` を同期実行
  ── `buildGridEvolution` は約 20〜35 ms／stage 数上限で頭打ちのため throttle 不要 ──
  Preview ボタンと同じく最終ステージを表示）、「▶ Preview grid evolution」＋ 🎲 reseed、
  ステージ scrub 用 range スライダー（`.ce-grid-step`）＋ `⏮◀▶⏭`、ステージラベル
  （`.ce-grid-evo-status`）、Voronoi/Delaunay/Sites トグル、「この格子を採用」。プレビューは
  `documentState` を触らず overlay 描画のみ。採用は `meshFromCells(stage.cells)` →
  featureGroups/gates/elements を空にして `history.commit`（undo 復元可）。`commit()` /
  New grid / import / undo で stale プレビューを自動 dismiss。
- `index.html`：`.ce-grid-*` CSS（`.ce-grid-slider-control` 追加）。
- テスト：`gen/patches.test.ts`（9）、`gen/gridEvolution.test.ts`（7）、
  `ui/CityEditorPage.grid.test.ts`（7）追加。検証：`tsc` 0、`biome` 0、`lint:legacy` pass、
  `vitest src/city-editor src/city-generator src/city` = 32 files / 426 tests 緑、`npm run build` 成功。
  **ブラウザ未確認**（MCP Chrome プロファイルがロック中）。

**G1 で見送り（→ G1b / 後続、理由を明記）**：

- **Voronoi sites tool（`tool === "site"`：母点の add / move / delete）** ── `nPatches` 入力で
  「数の指定」は満たし、overlay で母点は可視。座標の直接編集はキャンバスのポインタ処理
  （2500 行の `CityEditorPage.ts` の pointerdown/move/up 全分岐）への侵襲が大きく、
  `buildPatches` 手本移植＋ステップ可視化＋ tuning という主目的とは分離できるため G1b に切る。
  `Tool` への `"site"` 追加も G1b。
- **`New grid` の既定を patches エンジンへ切替 ＋ `cellsAcross → nPatches` 読み替え** ──
  計画自身が「実測で調整／まず計測」としていた部分。`New grid` は現状 `buildGrid` のまま、
  patches 格子は Document パネルの「採用」で明示的に取り込む。実際の粗さ・見た目を評価して
  から既定を反転する（小さな追随変更）。
- `classifyUrban` の `urbanNPatches` 既定化（案 B）も同様に計測待ち（据え置き）。

---

## Phase G2 — 現実性フィルタ ＋ 街道の伸長方式（最優先バグ）

**目的**：`towngen-comparison.md §2.5 / §3.E`。海の上を街道が通る／海を城内に取り込む／
海にはみ出た建物、を pipeline 末尾で機械的に潰す。街道の遠方点をオプション化。

**手本**：TownGen には海が無いので手本は無い。`walkRiver` の `stop`/`trimAtWater`/`finalizeEnds`
（`gen/riverPath.ts`）を「roads にも同じ防御を」の雛形にする。

**対象**：新規 `core/gen/plausibility.ts`、`core/gen/streets.ts`、`core/gen/geom.ts`
（polyline×polygon クリップ・segment-in-polygon）、`core/generate.ts`（`runPlan` 末尾で適用）、
`ui/CityEditorPage.ts`（Generate パネルに `avoidSea` 等）、新規 `plausibility.test.ts`。

**アプローチ**（`towngen-comparison.md §3.E` の 1〜6）

1. `buildStreets` に `seaNodes: Set<number>`（海セル頂点）を渡し `nonUrban` で ∞ 化。
2. `farNodeFor` の goal が水中なら `goal→gate` 線分 × shoreline 交点の陸側 `cellSize` へ置換。
3. A\* が `null` の門は road を引かず `water:true` に再マーク（quay 描画）。
4. 生成済み `roads`/`arteries` を最終検査：`waterPolygon` 内区間があれば drop か shoreline クリップ。
5. 城壁：`borders` 各辺 mid が水中なら区分を `coast` に、海側は塞がない。建物：`withinCell` に
   `&& !pointInPolygon(centroid(piece), waterPolygon)`。門：外側 `cellSize·1.5` が全 sea なら water gate。
6. road ゼロの land gate が過半なら派生 seed で synth 道路方位を作り直し 1 回リトライ。
7. `GenerationSettings.streets = { farNode: "descriptorEnd"|"radial"|"manualBearings", manualBearings?, avoidSea: true, foldSmoothing: true }` を追加。`avoidSea` で 1〜6 を有効化。

**受入条件**

- 4 海岸シナリオ（straight / bay / cape / 海＋川）× 複数 seed で、road・artery・building・wall の
  いずれの区間も `waterPolygon` を跨がない。
- `avoidSea` を off にすると従来の（跨ぐ）挙動に戻る（切り分け可能）。
- `farNode` 3 モードで街道の遠方端が仕様どおり（記述子端 / 放射 / 手動方位）。
- `tsc` 0・`biome`・`vitest` 全緑。

**テスト**：`plausibility.test.ts`（上記 assert）、`gen/streets.test.ts` 追記
（`seaNodes` を渡すと A\* 経路に海頂点が現れない、`farNode` モード別の端点方位）。

**G2 実施結果（2026-09-05）**：完了。

- `core/gen/geom.ts`：`segmentSegmentHit` / `segmentPolylineIntersection` /
  `segmentInteriorInPolygon` / `clipPolylineOutsidePolygon` / `longestDryRun`
  （polyline × polygon クリップ。境界だけの接触は跨ぎに数えない）。
- 新規 `core/gen/plausibility.ts`：`landwardFarNode`（goal→gate × shoreline 交点の
  陸側 `cellSize`）、`clipPolylinesToLand`、`remakeUnreachableLandGates`、
  `majorityLandGatesUnserved`、`splitDryWallRuns`（海側辺を落として連続 run に分割、
  `validate` の contiguous 制約を守る）、`filterBuildingsOverWater`（S7 未配線だが
  `withinCell` 相当を用意）。
- `core/gen/streets.ts`：`avoidSea`（既定 true）で海頂点を `hardBar` に載せる／off で
  従来どおり海を通れる。`farNode` 3 モード（`descriptorEnd` / `radial` /
  `manualBearings`）。放射スタブの mid が水中なら付けない。G7 の河川 `hardBar` は
  論理和のまま。
- `core/generate.ts`：`GenerationSettings.streets`。`runPlan` 末尾でクリップ・
  門の water 再マーク・道路方位 1 回リトライ。`applyPlan` は湿った城壁辺を run 分割。
  `avoidSea` off では `markSeaSurroundedGates` もスキップ。
- `ui/CityEditorPage.ts` Generate パネル：☑ Avoid sea、Street far end セレクト、
  Manual bearings テキスト（当該モードのときだけ表示）。
- テスト：`plausibility.test.ts`（13）、`streets.test.ts` に G2 describe（6）、
  Generate パネル UI 1。検証：`tsc` 0、`biome` クリーン、`vitest src/city-editor`
  15 files / 248 tests 緑。`src/city-generator/` は未変更（凍結）。

---

## Phase G3 — 門のオプション化

**目的**：`towngen-comparison.md §2.3 / §3.C`。門本数モデル・間隔・候補・外郭 split を UI から。

**手本**：`CurtainWall.buildGates` [CurtainWall.ts:39-100]（`entrances` = 2 パッチ以上共有の
円周頂点、`do…while(entrances.length>=3)` で 1 個選んで周囲 splice、`outer.shape.split(gate,
farthest)` で外郭割り、`gate.set(smoothVertex(gate))`）。

**対象**：`core/gen/gates.ts`（`placeGates` 移植＋拡張）、`core/gen/site.ts`（`synthSite` 移植、
川 `±90` 追加を「市街横断の川のみ・最大 1 本」に制限）、`ui/CityEditorPage.ts`、`gates.test.ts`。

**アプローチ**

```ts
GenerationSettings.gates = {
  countModel: "shape" | "roads" | "manual",   // 既定 "roads"
  manualCount?: number,
  minSpacingFactor: number,                    // 既定 1.0（弧長 perimeter/(target+1) の係数）
  candidates: "corners" | "anyBorderVertex",   // 既定 "corners"
  outerSplit: boolean,                         // 既定 true
}
```

- `"shape"`：border 頂点で「2 urban セル共有」の角のみ候補、`entrances.length>=3` の間ループ
  しつつ周囲を splice（`CurtainWall` 相当）。本数は形状で決まる（概ね 3〜5）。
- `"roads"`：現状の `target = clamp(round(suggestedGates·0.7), 3, 6) + (wet?1:0)`。
- `"manual"`：`manualCount` をそのまま target に。
- `minSpacingFactor` を `spacing = loopLength/(target+1) · factor` に噛ませ、間引き半径を可変に。
- `candidates:"anyBorderVertex"` は corner フィルタを外す（粗いメッシュで角が足りない時の保険）。
- `outerSplit`：門ごとに外側 rural セルを `splitFace(faceId, gate近傍v, farthest v)` で 1 回割る。
  City Editor は `splitFace` を持つのでメッシュ操作として実装可能。road の抜けを保証。
- `◀/▶` の `generateGateStep` は既存のまま（門 1 個ずつ）。

**受入条件**

- `countModel` 3 種で門数が仕様どおり（shape=形状由来 3〜5、roads=従来、manual=指定値）。
- `minSpacingFactor` を上げると門が減り、等間隔性が上がる。
- `candidates:"corners"` では全門が 2-urban 共有頂点に載る。
- `outerSplit:true` で各門の外に新セルが 1 個でき、その門の road が必ず外周へ抜ける。
- `tsc` 0・`biome`・`vitest` 全緑。`interior.test.ts` 相当の移植テスト緑。

**テスト**：`gen/gates.test.ts`（上記）、`gen/site.test.ts`（川 `±90` 追加が横断川のみ・1 本）。

---

## Phase G4 — 河川のランダム生成オプション

**目的**：`towngen-comparison.md §2.4 / §3.D`。本数・通過形態・蛇行・合流・幅・追従を UI から。
頂点列＝真実源（design §3.1 / §3.2 B）を厳守し、水域内 river edge を作らない。

**手本**：TownGen に河川は無い。`gen/riverPath.ts walkRiver` と `gen/site.ts synthRivers` の
既存パラメータ（`wander` / `meanderScale` / `corridorPull` / `widths` / `crossesSite`）を露出。

**対象**：`core/gen/rivers.ts`（`walkRiver` ラッパ）、`core/gen/site.ts`（`synthRivers`）、
`core/gen/classifyRiver.ts`、`core/generate.ts`（`applyPlan` の river group 化）、
`ui/CityEditorPage.ts`、`rivers.test.ts`。

**アプローチ**

```ts
GenerationSettings.rivers = {
  count: number,                                 // 0..3
  passage: ("through" | "beside" | "toCoast")[], // 河川ごと。未指定は count と coast から導出
  meander: number,                               // walkGraph の wander、0..1、既定 0.7
  confluence: boolean,                           // 2 本目以降を 1 本目へ合流、既定 false
  widthScale: number,                            // 既定 1.0
  corridorPull: number,                          // 既定 1.5（小さいほど暴れる）
}
```

- `riversForCount` を `passage[]` ベースに一般化（`through` は `synthSite` の through-family、
  `beside` は beside-family、`toCoast` は海がある時のみ有効）。
- `confluence`：2 本目の walk goal を 1 本目の `edgePoints` 上の点に設定。最初に触れた共有
  メッシュ頂点で `RiverGroup.mouth = { kind: "river", targetId: <1本目 group id> }`。下流は別 group。
  `mesh.ts validate` の river 連続性チェックはそのまま通る（共有頂点は実頂点）。
- `meander` / `corridorPull` / `widthScale` を `walkRiver` / `resampleWidths` に直結。
- `applyPlan` の river group 化（`polylineToVertexPath`）は既存のまま。
- `◀/▶` の `generateRiverWalkStep` は既存のまま（複数河川スクラブ対応済み）。

**受入条件**

- `count` どおりの本数。`passage` 別に `classifyRiver` の bank 成分数が変わる
  （`through` で市街が 2 成分に割れる）。
- `meander` を上げると walk の総折れ角が単調増加。
- `confluence:true` で 2 本目の `mouth.vertexId` が 1 本目の頂点列に含まれる。
- どの設定でも `RiverGroup` は water face 接続頂点／map boundary／他 River 終端以外で
  終わらない（`validate` エラー 0）。水域内に導出 edge を持たない。
- `tsc` 0・`biome`・`vitest` 全緑。

**テスト**：`gen/rivers.test.ts`（上記）、`riverPath.test.ts` 相当の移植テスト緑。

---

## Phase G5 — セル辺の平滑化仕上げ

**目的**：`towngen-comparison.md §2.6 / §3.F`。街路に乗っていない街区境界のカクカクを緩和。
※ G1 が効いていれば影響は小さい ── **まず計測してから着手**。

**手本**：`Model.buildStreets` の `smoothStreet`＝`smoothVertexEq(3)` を共有 `Point` へ書き戻す
[Model.ts:293-298, 334-336]。

**対象**：`core/gen/streets.ts`（`foldArteriesIntoCells` 相当）、`core/gen/edgeGraph.ts`
（`smoothPath` の f 調整）、`core/features.ts`（`smoothFeatureGroup` の generate 直後自動適用）。

**アプローチ**

1. まず G1 後の実メッシュで「街路非接触の短辺総長」「街区境界の総折れ角」を計測ログ。
   改善余地が小さければこのフェーズは skip 判断。
2. `smoothPath` 既定を 2 → 3 反復、または `f` を TownGen の 3 に
   （`(p[i-1] + 3p[i] + p[i+1])/5`）。
3. `runPlan` の S5 後：平滑化した artery/street 頂点を対応メッシュ頂点へ書き戻す。City Editor の
   route は `EdgeRef[]` / 頂点列なので、`smoothFeatureGroup`（`features.ts`）を road/wall/river
   グループへ generate 直後に自動適用する形が最小変更。reserved（border/citadel/gate/river/coast）は
   `pinnedSmoothingVertices` で固定（既存）。
4. 必要なら reserved 以外の全 interior 頂点へ弱 Laplacian 1〜2 パス（新規 `gen/relaxMesh.ts`）。

**受入条件**：街区境界の総折れ角が計測で有意に減少（目安 −30% 以上）、reserved 頂点が不動、
feature group の連続性・`validate` エラー 0、undo 復元。`vitest` 全緑。

**テスト**：`gen/streets.test.ts` 追記（fold 後に street 頂点とメッシュ頂点が一致、reserved 不動）。

---

## Phase G7 — 河川辺の排他（河川・街道の同一 edge 禁止）

**目的**：改善計画「河川・街道・外壁が同一 edge 上で重なる不具合」。三者の重なりは、
河川が使うセル辺の上を街道 A\* が歩けることが直接の原因である。`design.md` §3.1 の
「river + road は同一 edge を拒否」を生成経路へ載せる。河川辺は **高コストではなく禁止**
（端点でも免除しない `hardBar`）。壁＋河川は §3.1 どおり許可（岸壁／水門）のまま残し、
街道がその辺に乗れなくなった時点で三重重ねは構造的に起きない。

**手本**：TownGen に河川は無い。既存の `hardBar`（城郭 enceinte、海セル頂点）が雛形。
`weight` の `Infinity` は端点ホップが免除されるので使わない。

**対象**：`core/gen/streets.ts`（`StreetInputs` に河川辺、A\* `hardBar`）、
`core/generate.ts`（`runPlan` から河川 `edgePoints` を渡す、`applyPlan` の防御、
任意で `validate` 相当の検査）、`core/features.ts`（手動編集の同じ不変条件）、
`gen/streets.test.ts` / `generate.test.ts` / `features.test.ts`。

**非目標**

- 壁を河川辺から外す／岸壁へ自動変換する（§3.1 は wall + river を許可）。
- wall + road の共有辺（門頂点での接続は現状どおり。本フェーズは河川辺だけ）。
- Generate パネルのトグル。常時禁止。G2 の `avoidSea` とは独立。

**アプローチ**

1. **河川が使う辺の定義**  
   `walkRiver` の `edgePoints`（実セル辺の頂点列）で、連続する 2 点を
   `MERGE_QUANTUM` でグラフ頂点へ写し、無向辺 `{min(a,b), max(a,b)}` の集合にする。
   平滑化後の `smoothPoints` は使わない（格子から外れる）。河川 0 本なら空集合。
2. **A\* は禁止、高コストではない**  
   `buildStreets` の `StreetInputs` に `riverEdges`（または `rivers: Point[][]`）を追加。
   streets（門→広場）と roads（遠方→門）の両方で、既存 `hardBar` と論理和する。

   ```ts
   const onRiver = (a: number, b: number): boolean =>
     riverEdgeSet.has(undirectedKey(a, b));
   // roads:  hardBar = crossesSea || onRiver
   // streets: hardBar = crossesCitadel || onRiver
   ```

   `hardBar` は端点ホップも免除しない（`streets.ts` 既存コメントどおり）。門が河川頂点に
   載っていても、**河川辺そのものへの第一歩は打てない**。河川頂点へ **別辺から到着する**
   のは可（§3.1「交差は共有頂点。橋は将来拡張」）。
3. **A\* が `null` の門**は road / street を引かない。河川辺を歩いて到達するフォールバックは置かない。
4. **`applyPlan` の防御**  
   ② で書いた `RiverGroup` から導出 edge（連続 vertex 対の `edgeBetween`）を集め、
   ⑤ の `polylineToEdgeRefs` 結果からその `edgeId` を落とす。連続が切れたら最長の連続
   run だけ残す（A\* が守っていてもスナップが河川辺へ落ちる漏れ用）。
5. **手動編集**（同じ不変条件）：`appendEdge`（road）は対象 edge を河川が使っていれば
   `null`。`appendRiverVertex` は追加区間の edge を road が使っていれば `null`。
   wall との共有は変更しない。
6. **G2 との合成**：G2 が `crossesSea` を roads の `hardBar` に載せる。G7 はそれに
   `onRiver` を OR するだけ。`weight` 側へ河川コストを足さない。

**受入条件**

- 河川ありの複数 seed（内陸 through / 海岸 toCoast / 市街縁 beside）で、⑤ 後のどの
  `gc:road-*` も、どの `gc:river-*` 導出 edge とも `edgeId` を共有しない。
- 市街外周が河川と重なるケースでも、wall はその辺に乗ってよいが road は乗らない
  （三重重ねが消える）。
- 門頂点が河川頂点でも、その門の road の第一辺は河川辺ではない。
- 河川 0 本では streets / roads の本数・形状が G7 前とバイト一致（回帰）。
- 手動で road を河川辺へ延長すると拒否。逆も同じ。
- `tsc` 0・`biome`・`vitest` 全緑。ブラウザで console エラー無し。

**テスト**

- `gen/streets.test.ts`：`edgePoints` で作った `riverEdges` を渡すと、roads / streets の
  どの連続頂点対もその集合に入らない。`weight` ではなく `hardBar` であることの証拠として、
  門＝河川頂点でも第一ホップが河川辺にならない。河川辺しか通路が無い配置ではその road が
  空（`null` → 未出力）。
- `generate.test.ts`：stage ⑤ の document で river 導出 `edgeId` ∩ road `segments.edgeId`
  が空。wall ∩ river は空でなくてよい。河川 0 本は既存フィクスチャと一致。
- `features.test.ts`：road の `appendEdge` が河川辺を拒否、`appendRiverVertex` が road 辺を拒否、
  wall の河川辺追加は成功。

**G7 実施結果（2026-09-05）**：完了。

- `core/gen/streets.ts`：`StreetInputs.rivers`（`walkRiver` の `edgePoints`）を追加。連続点を
  `MERGE_QUANTUM` でグラフ頂点へ写し、無向辺を `hardBar` に載せる（streets = 城郭 ∨ 河川、
  roads = 海 ∨ 河川）。`weight` には足さない。門への放射スタブも `onRiver` なら付けない。
  A\* が `null` の門は road / street を出さない。
- `core/generate.ts`：`runPlan` が `rivers.map(b => b.edgePoints)` を渡す。`applyPlan` は
  ② の `RiverGroup` 導出 edge を集め、⑤ のスナップ結果から最長の非河川連続 run だけ残す。
- `core/features.ts`：`appendEdge`（road）は河川辺を拒否。`appendRiverVertex` は road 辺を拒否。
  wall は河川辺に乗せてよい。
- テスト：`gen/streets.test.ts`（5）、`generate.test.ts` に G7 describe（2）、
  `features.test.ts` に手動編集 3 件。`CityEditorPage.generate.test.ts` の `seedRichTown` は
  街道も要求するよう更新（G7 後に road 0 の町が ⑤ スクラブを空振りしないため）。
- 検証：`biome` クリーン、`vitest` 上記 159 テスト緑。`src/city-generator/` は未変更（凍結）。

**G7 追補（2026-09-05）**：同一 vertex での門・橋通り抜け。辺の共有は禁止のまま、次数 4 以上で対角辺を使う通り抜けは許可。次数 4 未満で道路が壁／河川と出会う頂点は、バリア上の最寄り頂点へ `mergeVertices`、失敗時は入射セルを `splitFace` して次数を上げ、道路を対角辺へ通す（`core/passages.ts`）。⑤ 以降は局所的な merge/split でメッシュが変わり得る（格子の作り直しではない）。

---

## Phase G6 — `city-generator` 撤去とページ切替（最後）

**目的**：凍結モジュールを実際に消す。改善計画「廃棄予定」の完了。

**前提**：G0〜G5 および G7 完了。`/city-editor/` が生成品質・オプションで `/city/` を上回っていること。

**アプローチ**

1. FMG Burg エディタ：`src/controllers/burg-editor.ts openCityGenerator()` の遷移先を
   `${BASE_URL}city/` → `${BASE_URL}city-editor/` に変更。sessionStorage handoff の
   キー／descriptor 契約は据え置き（`incomingSite` 相当を City Editor 側に用意 ── design §7 の
   「FMG Burg handoff（後続）」）。UI ラベル・`id="burgOpenCityGenerator"` を必要に応じ調整。
2. `vite.config.ts` の `rollupOptions.input` から `city` を削除。
3. `src/city/`、`src/city-generator/` を削除。`docs/city-generator/` は
   `towngen-comparison.md`（分析・パス据え置き）を残し、実装済みメモは `design.md` 側へ集約
   （`draft.md` / `wall-patterns.md` / `water-context.md` などの参照は City Editor 文書へリンク張り替え）。
4. 残った参照を grep で 0 確認（`city-generator` / `'city/'`）。`tsc` / `biome` / `vitest` /
   `build` 全緑、world/d3/three 参照 0（既存の payload チェック）。

**受入条件**：`src/` 全体で `city-generator` 参照 0、Burg エディタから City Editor が開き
descriptor を受け取れる、build 成功、既存の world-map bundle 分離が保たれる。

---

## 横断事項

- **命名/衝突**：`gen/` に集めると `clone` / `Point` / `geom` など既存 `city-editor/core` と
  名前が被る。`gen/` 内で完結させ、`generate.ts` からは名前付き import で明示。
- **テスト移設**：G0 で `city-generator/core/*.test.ts` のうち City Editor が使う関数の分を
  `gen/` へコピー。以後の改良テストはここに足す。
- **決定論の担保**：各フェーズで「同一 `(document, settings, seed)` → 同一 JSON」を 1 ケース
  以上 assert。seed 由来の RNG stream を分岐させる時は `makeRng(\`${seed}:...\`)` の派生キー方式
  （既存 `runPlan` と同じ）。
- **UI 配置**：Grid evolution は **Document パネル**（格子の話）、門・河川・街道オプションは
  **Generate パネル**（`GenerationSettings` の一部）。既存の step 行部品を流用し新規 CSS を増やさない。
  G7 の河川辺禁止はノブを出さない（常時）。
- **`generate.ts` の現行 6 スクラブ関数**（`generateCoastWalkStep` … `generateWardStep`）は
  G0 で import 先が変わるだけ。G3/G4 でオプションが増えても `settings` 経由なのでシグネチャ不変。
- **GPL 再確認**：各 `gen/*.ts` の冒頭コメントに「TownGeneratorTS を手本にした再実装。GPL
  コードは含まない」を明記（`src/city-generator/LICENSE-NOTE.md` の方針を踏襲）。

## 未決 / 要相談

- G1 の `nPatches` と `CITY_SIZE_PRESETS`（Small/Medium/Large）の対応表 ── 実測で決める。
- G5 を実施するか（G1 の効果次第、計測ログで判断）。
- G6 のタイミング（G1〜G4 のうちどこまで揃えば standalone ページを畳むか）。
- FMG handoff（descriptor 受け取り）を G6 に含めるか、別マイルストーン（design §7-3）にするか。
